import { Button, SpanButton } from "./styles";

const ButtonBack = () => {
  return (
    <Button className="btn">
      Voltar <SpanButton></SpanButton>
    </Button>
  );
};

export default ButtonBack;
